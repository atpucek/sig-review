# Smartsheet API token (server-side only, never exposed to browser)
SMARTSHEET_API_TOKEN=

# Vercel KV (production only — locally the app falls back to .data/previews.json)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
